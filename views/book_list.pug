extends layout

block content
	h1= title

	ul
	each arbitrarywordhere in book_list
		li
			a(href=arbitrarywordhere.url) #{arbitrarywordhere.title}
			| (#{arbitrarywordhere.author.name})

	else
		li There are no books.